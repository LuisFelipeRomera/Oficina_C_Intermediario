#include<stdio.h>
#include<array.h>

int sumArr(int *arr, int n){

    int total = 0;

    for (int i = 0; i < n; i++) {

        total += arr[i];
    }

    return total;
}

int main(){

    int a;

    printf("Input the size of the array: ");
    scanf("%d", &a);

    int arr[a] = {};

    for (int i = 0; i < )
}
